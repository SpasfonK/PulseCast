# Règles ProGuard/R8 spécifiques à PulseCast.
#
# Vide pour la Phase 1 : isMinifyEnabled = false sur les deux build types,
# donc ce fichier n'est pas encore exercé. À compléter dès qu'un build
# release minifié sera activé (règles Media3/ExoPlayer, Room, Coil).
